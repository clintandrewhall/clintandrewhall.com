//vimport { createRoutesFromElements, Route, Routes } from 'react-router';

// import { App } from '../app';

// const allRoutes = (
//   <Route element={<App />}>
//     <Route path="/" lazy={async () => ({ Component: (await import('../pages/home')).Home })} />
//     <Route
//       path="/portfolio"
//       lazy={async () => ({ Component: (await import('../pages/portfolio')).Portfolio })}
//     />
//     <Route {...portfolioItem} />
//   </Route>
// );

// export const AppRoutes = <Routes>{allRoutes}</Routes>;

// export const routes = createRoutesFromElements(allRoutes);
